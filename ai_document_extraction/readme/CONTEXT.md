Accounting teams receive invoices in many formats. Reading them manually is slow
and error prone. This module automates the initial data-entry step while keeping a
human in the loop: the extraction is applied to a draft move that a user reviews
and posts. All AI components run on-premises (Ollama), so document data never
leaves the local infrastructure when a local endpoint is used.
