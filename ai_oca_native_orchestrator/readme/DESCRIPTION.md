# Native AI Orchestrator

Provides the core Orchestrator Agent engine for multi-agent LLM planning, intent extraction, configurable system prompts (`ai.prompt.template`) rendered via QWeb, and persona management (`ai.persona`).
Persists structured JSON outputs on sub-agent task threads (`ai.task.thread` / `ai.task.message`).
